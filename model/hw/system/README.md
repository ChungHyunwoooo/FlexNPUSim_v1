# model/hw/system

System-composition configs (reserved). The v2 schema is being designed in
docs/reference/system/topology.md; the old bus-topology JSONs were retired with
the unused assembly machinery (git history has them).
